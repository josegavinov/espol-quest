# Copie este archivo como .env y ajuste los valores.

# Si no define DATABASE_URL el servicio usa SQLite (archivo local, sin instalar nada).
# PostgreSQL 15 (stack objetivo del proyecto):
# DATABASE_URL=postgresql+psycopg://espol:espol@localhost:5432/espol_quest_game

PORT=5001
